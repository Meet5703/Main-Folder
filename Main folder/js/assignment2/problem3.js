// Program for check a , b , c value is positive negative and zero 

let a = 10 , b = -20 , c = 0

a > 0 ? console.log(`${a} is Positive`):
a < 0 ? console.log(`${a} is Negative`):
console.log(`${a} is Zero`);

b > 0 ? console.log(`${b} is Positive`):
b < 0 ? console.log(`${b} is Negative`):
console.log(`${b} is Zero`);

c > 0 ? console.log(`${c} is Positive`):
c < 0 ? console.log(`${c} is Negative`):
console.log(`${c} is Zero`);


